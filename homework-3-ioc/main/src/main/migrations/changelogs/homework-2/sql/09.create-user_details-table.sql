CREATE TABLE user_details
(
    user_id     INT PRIMARY KEY,
    name        VARCHAR NOT NULL,
    surname     VARCHAR NOT NULL,
    phone       VARCHAR NOT NULL,
    work_phone  VARCHAR NOT NULL,
    work_adress VARCHAR NOT NULL,
    department  VARCHAR NOT NULL

);
