CREATE TABLE attachments (
                       id INT PRIMARY KEY,
                       title VARCHAR NOT NULL,
                       path VARCHAR NOT NULL ,
                       task_id INT NOT NULL
);
