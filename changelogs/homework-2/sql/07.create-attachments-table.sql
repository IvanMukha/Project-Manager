CREATE TABLE attachments (
                       id INT PRIMARY KEY,
                       title VARCHAR(50) NOT NULL,
                       path VARCHAR(50) NOT NULL ,
                       task_id INT NOT NULL
);
